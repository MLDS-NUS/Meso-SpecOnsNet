defaults:
  - basic
  - viz/convdiff1d/rollout@_here_
  - viz/pred@_here_

model_checkpoint:
  dirpath: ${paths.output_dir}/checkpoints
  filename: "epoch_{epoch:03d}"
  monitor: "valid/loss"
  mode: "min"
  save_last: True
  auto_insert_metric_name: False
