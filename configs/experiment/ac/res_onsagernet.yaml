# @package _global_

# to execute this experiment run:
# python train.py experiment=ac/res_onsagernet

defaults:
  - ac/share
  - override /model: res_onsagernet

logger:
  wandb:
    notes: "res_onsagernet"

hydra:
  run:
    dir: ${paths.log_dir}/${task_name}/runs/ac/res_onsagernet
