# @package _global_

# to execute this experiment run:
# python train.py experiment=ac/fno

defaults:
  - ac/share
  - override /model: fno

logger:
  wandb:
    notes: "fno"

hydra:
  run:
    dir: ${paths.log_dir}/${task_name}/runs/ac/fno
