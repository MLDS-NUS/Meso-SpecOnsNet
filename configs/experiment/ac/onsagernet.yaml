# @package _global_

# to execute this experiment run:
# python train.py experiment=ac/onsagernet

defaults:
  - ac/share
  - override /model: onsagernet

logger:
  wandb:
    notes: "onsagernet"

hydra:
  run:
    dir: ${paths.log_dir}/${task_name}/runs/ac/onsagernet
