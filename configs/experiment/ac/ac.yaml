# @package _global_

# to execute this experiment run:
# python train.py experiment=ac/ac

defaults:
  - ac/share
  - override /model: ac

logger:
  wandb:
    notes: "ac"

hydra:
  run:
    dir: ${paths.log_dir}/${task_name}/runs/ac/ac
