# @package _global_

# to execute this experiment run:
# python train.py experiment=ac/s_onsagernet

defaults:
  - ac/share
  - override /model: s_onsagernet

logger:
  wandb:
    notes: "s_onsagernet"

hydra:
  run:
    dir: ${paths.log_dir}/${task_name}/runs/ac/s_onsagernet
