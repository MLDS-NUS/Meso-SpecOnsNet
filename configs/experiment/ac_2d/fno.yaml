# @package _global_

# to execute this experiment run:
# python train.py experiment=ac/fno

defaults:
  - ac_2d/share
  - override /model: fno2d

logger:
  wandb:
    notes: "fno"

hydra:
  run:
    dir: ${paths.log_dir}/${task_name}/runs/ac_2d/fno
