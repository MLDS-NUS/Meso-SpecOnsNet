# @package _global_

# to execute this experiment run:
# python train.py experiment=fput/onsagernet

defaults:
  - fput/share
  - override /model: onsagernet

logger:
  wandb:
    notes: "onsagernet"

model:
  dynamics:
    dissipation:
      beta: 0.0

hydra:
  run:
    dir: ${paths.log_dir}/${task_name}/runs/fput/onsagernet
