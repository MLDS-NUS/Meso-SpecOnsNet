# @package _global_

# to execute this experiment run:
# python train.py experiment=fput/s_onsagernet

defaults:
  - fput/share
  - override /model: s_onsagernet

logger:
  wandb:
    notes: "s_onsagernet"

hydra:
  run:
    dir: ${paths.log_dir}/${task_name}/runs/fput/s_onsagernet
