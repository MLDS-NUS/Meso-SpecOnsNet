# @package _global_

# to execute this experiment run:
# python train.py experiment=fput/res_onsagernet

defaults:
  - fput/share
  - override /model: res_onsagernet

logger:
  wandb:
    notes: "res_onsagernet"

hydra:
  run:
    dir: ${paths.log_dir}/${task_name}/runs/fput/res_onsagernet
