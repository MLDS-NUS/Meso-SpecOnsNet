# @package _global_

# to execute this experiment run:
# python train.py experiment=fput/fno

defaults:
  - fput/share
  - override /model: fno

logger:
  wandb:
    notes: "fno"

hydra:
  run:
    dir: ${paths.log_dir}/${task_name}/runs/fput/fno
