# @package _global_

# to execute this experiment run:
# python train.py experiment=fene/res_onsagernet

defaults:
  - fene/share
  - override /model: res_onsagernet

logger:
  wandb:
    notes: "res_onsagernet"

hydra:
  run:
    dir: ${paths.log_dir}/${task_name}/runs/fene/res_onsagernet
