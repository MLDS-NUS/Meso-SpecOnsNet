# @package _global_

# to execute this experiment run:
# python train.py experiment=kdv/kdv

defaults:
  - kdv/share
  - override /model: kdv

logger:
  wandb:
    notes: "kdv"

hydra:
  run:
    dir: ${paths.log_dir}/${task_name}/runs/kdv/kdv
