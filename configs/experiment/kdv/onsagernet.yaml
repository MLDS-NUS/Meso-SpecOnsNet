# @package _global_

# to execute this experiment run:
# python train.py experiment=kdv/onsagernet

defaults:
  - kdv/share
  - override /model: onsagernet

logger:
  wandb:
    notes: "onsagernet"

model:
  dynamics:
    dissipation:
      beta: 0.0

hydra:
  run:
    dir: ${paths.log_dir}/${task_name}/runs/kdv/onsagernet
