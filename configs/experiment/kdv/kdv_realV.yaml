# @package _global_

# to execute this experiment run:
# python train.py experiment=kdv/kdv_realV

defaults:
  - kdv/share
  - override /model: kdv_realV

logger:
  wandb:
    notes: "kdv_realV"

hydra:
  run:
    dir: ${paths.log_dir}/${task_name}/runs/kdv/kdv_realV
