# @package _global_

# to execute this experiment run:
# python train.py experiment=kdv/res_onsagernet

defaults:
  - kdv/share
  - override /model: res_onsagernet

logger:
  wandb:
    notes: "res_onsagernet"

hydra:
  run:
    dir: ${paths.log_dir}/${task_name}/runs/kdv/res_onsagernet
