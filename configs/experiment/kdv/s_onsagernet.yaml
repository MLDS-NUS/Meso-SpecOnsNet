# @package _global_

# to execute this experiment run:
# python train.py experiment=kdv/s_onsagernet

defaults:
  - kdv/share
  - override /model: s_onsagernet

logger:
  wandb:
    notes: "s_onsagernet"

hydra:
  run:
    dir: ${paths.log_dir}/${task_name}/runs/kdv/s_onsagernet
